node_modules/
.expo/
.eas/
dist/
coverage/
android/
ios/
*.log
.DS_Store

# ==========================================================================
# 隐私：任何图像 / 视频 / 真值数据一律不入仓
#
# 2026-08-28 事故：编码格式从 JPEG 改成 BMP 时，改了代码、改了文档，
# 忘了改这里的 `verify/long_node.jpg`，于是 25.9MB 的真实微信聊天长图
# 进了三个 commit，由 Codex 冷读时发现。仓库当时没有 remote，才来得及处理。
#
# 教训：**不要按文件名拦，按类型拦。** 文件名会跟着实现改，规则不会自动跟上。
# ==========================================================================
*.bmp
*.png
*.jpg
*.jpeg
*.mp4
*.mov
*.mkv
*.webp
*.heic
*.heif
*.avif
verify/groundtruth.json
verify/expected_output.md
verify/out/
verify/frames/
verify/*.md.out
