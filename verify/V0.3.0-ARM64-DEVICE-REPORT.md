# v0.3.0 arm64 精简包 真机运行记录

**日期**：2026-09-03
**设备**：OPPO Find N6（与 v0.3.0 验收同一台）
**安装包**：`tenglu-v0.3.0-arm64.apk`，42,095,369 bytes，SHA-256 `ed8fba2a3be6479e9150dd46d37dabc6b0500c2142d32e9e68945073747a1402`
**构建**：EAS build `0ee23074-67b6-425d-bd44-0cb52340241f`，profile `preview-arm64`，源 commit `ca3daeb`
（应用代码与 v0.3.0 发布包同源；`eas.json` 只加了一个 profile，用 `-PreactNativeArchitectures=arm64-v8a` 过滤架构）

## 与全量包的静态对比

| 项 | 全量包 `tenglu-v0.3.0.apk` | arm64 包 |
|---|---|---|
| 体积 | 109.3 MiB | 40.1 MiB（−63.3%） |
| `lib/` | armeabi-v7a / arm64-v8a / x86 / x86_64 | 仅 arm64-v8a |
| 非 `lib/`、非 `META-INF/` 条目 | 1161 项 | 1161 项，CRC 与大小逐项一致 |
| `lib/arm64-v8a/*.so` | 15 个 | 15 个，CRC 逐个一致 |

## 真机运行（Alice 手动安装并操作，报告由 App「复制完整验收报告」导出）

素材：一段 111 秒的小红书录屏（图文笔记 + 点开的评论区），通用模式，处理路径保持默认文本锚点。
这段素材比 README「实际测过的范围」里最长的 44.2 秒长得多，此处只作为 arm64 包能否正常运行的证据，
**不作为准确率数据**——它没有真值，也没有和全量包做逐字节对比。

```
路径: 文本锚点（M3）
请求路径: 文本锚点（M3）
状态: ok
模式: 通用

耗时:
精确帧抽取: 29217 ms
逐帧 ML Kit OCR: 20057 ms
UI 让出: 1411 ms
固定 UI / 锚点 / 去重: 1693 ms
歧义气泡局部采样: 0 ms
Markdown: 0 ms
临时文件清理: 50 ms
总耗时: 52440 ms
计时对账: 通过

还原告警:
无
```

统计（节选自报告的 JSON）：

| 字段 | 值 |
|---|---|
| fps / stride / maxAnchorGapMs | 4 / 3 / 750 |
| sourceFrameCount / extractedFrameCount | 445 / 149 |
| frameWidth × frameHeight | 720 × 1652 |
| anchorPairCount / anchorPassedCount | 148 / 148 |
| cumulativeShift | 17,154 px |
| ocrLineCount / contentLineCount / uniqueLineCount | 5,113 / 4,030 / 570 |
| frameExtractionMethod | `MediaMetadataRetriever.OPTION_CLOSEST` |
| nativeFrameExtractPerFrameMs | 196.1 |
| timingAccountedMs / timingDeltaMs / timingThresholdMs | 52,429 / 12 / 524 |
| remainingTempFileCount | 0 |
| screenAwakeRequested | true |
| durationMs | 111,325 |

`anchorDetails` 共 148 对，`shift` 在 0–307 px 之间，`voteRatio` 最低 0.75（`f_0229→f_0232`），
只有一对出现 `fuzzyRejected`（`f_0169→f_0172`，2 个候选被拒），其余全部接受；
报告开头 3 对 `shift: 0` 对应录屏起始的静止画面，末尾 3 对 `shift: 0` 对应滚到底后的静止。

报告的 Markdown 正文（约 570 行小红书评论区文字）**不入仓**：仓库惯例是测试素材与输出不进公开仓，
且这段内容属于第三方公开帖子。一处值得记录的观察：通用模式把状态栏时间（`09:49`/`09:50`）和
顶栏作者名混进了正文——固定 UI 的判定靠文本相等，而时钟在变、作者名的 OCR 结果在几种错字间跳，
所以没被当成固定带。这是核心逻辑层的事，本次产品化改造不动它，README 里如实写明。
