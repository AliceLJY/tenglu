# 誊录（Tenglu）— 施工图纸 v1

> 名字取自宋代科举的誊录制度：专人把考生原卷**一字不改地抄一遍**。
> 这正是本产品的全部承诺——只忠实还原，不加分析。名字待 Alice 确认，不影响施工。

## 一句话

把一段手机录屏，变成一份干净的 Markdown 记录。全程在手机本地完成，不联网、不调 API、不需要任何 key。

## 适用范围（先说清不适用什么）

**只支持竖屏手机的滚动截屏。** 明确不适用：

- **折叠机展开态**（OPPO Find N6 这类）。展开后接近方屏，一屏能装下更多内容，
  滚动一次的位移占比、气泡宽度、左右分栏都跟竖屏不是一回事；`PRESETS` 里的帧数与
  0.45–0.65 的位移区间都是按竖屏标定的。折叠机请**合起来录**。
- 横屏录制、平板、桌面端截屏。
- 非滚动内容（视频播放、动画、页面跳转）。素材里混进跳转段会让位移估计失效——
  实测小红书那段 9.8–10.8s 有一次页面跳转，conf 掉到 0.00–0.32。

**用户可见的一句话要求**：竖屏、匀速慢滑、中途不要切页面。

## 产品边界（已拍板，不要越界）

- **只做到"交出记录"**。不做分析、不做总结、不做洞察。用户拿这份 Markdown 去喂任何 AI，那之后的事与本产品无关。
- **不做语义猜测**。OCR 文字按位置输出，不根据词面或正则猜“这是点赞数 / 这是回复谁 / 这串字像昵称”。群聊昵称只有在 UI 本身把它渲染成气泡上方的独立小字行、几何条件完整时才作为显式字段保留；几何不清楚就维持原文，不删除内容。
  理由：读它的 AI 比正则更擅长这件事，而且我们一判断就会判错——2026-08-28 实测，精细解析 48 条评论错 4 条，全文本模式零错。
- **微信是唯一做左右区分的场景**（气泡颜色分我方/对方）。其它 app 一律纯文本。

## 已验证的事实（2026-08-28 实测，不要重新验证，也不要推翻）

用 Alice 真实录屏 + 微信解密记录做基准：

| 项 | 数字 |
|---|---|
| 输入 | 微信单聊录屏 27s / 720×1652 / 120fps / 2757 帧 |
| 取帧 | 固定 4fps 全取 **108 帧 → 6 个关键帧**（回弹帧 4 张被真实排除） |
| 位移估计（纯 JS，两级搜索） | **8–15 ms / 帧对**（Mac / Node 26） |
| 预扫（12 次取帧+解码+灰度+滚动区检测，`PRESETS.wechat`） | 约 380 ms |
| 取帧与位移（108 次取帧+解码+逐帧位移） | 约 4.4 s（其中解码约 2.6 s，是唯一大头） |
| 拼接（关键帧间一次性重匹配） | 约 85 ms |
| BMP 编码 | 约 30 ms |
| **拼接链路总耗时**（选定视频 → 长图落盘，不含 OCR） | **约 4.8 s** |
| 拼接长图 | **720×5107** |
| BMP 编码 | 11 ms / 10.5 MB |
| OCR（macOS Vision，分段 1400px + 重叠 200px） | 约 15 s |
| **字符级准确率** | **93.7%**（这是该报的数，正常抖动区间 93.7–94.0%） |
| 逐条完全一致 | 36 / 41 = 87.8%（**粒度太粗，实测在 87.8–92.7% 之间抖，不要对外报这个**） |
| 发言人判对 | 39 / 41 = **95.1%**（五组参数下一次没抖过） |
| 消息顺序 | 100% 正确 |

小红书笔记+评论录屏（32s / 2184 帧）同一套算法：逐帧位移置信度中位 0.987、**排除跳转段后**最低 0.93（跳转段那 4 个帧对是 0.00–0.32，见 4.5 节，正是 conf<0.5 防护的触发点）；早期验证拼出 720×11738 / 183 行 2489 字（**现行输出为 720×13558**——4.5 节跳转防护会把跳转段保守展开，去重后 199 行 2513 字）。
**结论：拼接层跨 app 通用，唯一要改的是固定 UI 的取样范围。**

## 核心算法（已验证，照抄，不要重新设计）

### 1. 分离固定 UI 与滚动区（滞后阈值，阈值自适应）

对预扫帧做逐行跨帧标准差 `row_std[y]`，然后**高阈值找种子、低阈值向外扩**（hysteresis）：

- 种子阈值 `hi = max(10, p85 × 0.70)`，扩展阈值 `lo = max(4, p85 × 0.28)`——
  `p85` 是本段自己的行方差 85 分位，**不写死数字**（固定 12.0 在滚动慢或对比度低的素材上会把内容区判成固定 UI）
- 用 `hi` 找最长连续段作种子，再用 `lo` 从种子向两端扩展，允许跨过 **<40 行**的短缺口（`GAP=40`，缺口达到 40 行即停）
- 返回的 `ok` 字段（内容区 ≥ 屏高 30%）**必须检查**，false 说明种子落错了地方

为什么不用更简单的两种做法（都各错一半，实测踩过）：
- 取"最大连续超阈值段"→ 下边界被内容区中部的局部走低截断（小红书 y183~1508 被判成 y1081~1195 只剩 114px）
- 从两端向内收缩 → 状态栏（时钟/电量/录屏胶囊也在逐帧变）被吃进内容区（两段素材都判成 y6~）
滞后阈值同时避开两者：中部走低（实测降到 15，两侧 22）高于 `lo` 能跨过去；状态栏与内容区之间隔着 std≈0 的导航栏，扩展到那里会停。

实测微信 y154–1522、小红书 y182–1509，与 Minis 的 iOS 独立实现互证差 0–1px。

⚠️ **取样只用预扫帧的后 60%。** 顶部导航栏在有些 app 里是滚动后才浮现的，用整段测会把它误判成滚动区。
⚠️ **预扫帧数按 `PRESETS[app].preScan`（微信 12 / 其它 24）。** 帧太少时种子会落进状态栏（小红书 12/16 帧时返回 y6~68）。

### 2. 位移估计（逐帧粗搜；关键帧两级精搜）

```js
const DS = 4;        // 粗搜降采样倍数
const OFF = 60;      // 模板在「新帧」中的起始 y
const BAND = 300;    // 模板高度
const R = 24;        // 精搜半径

// estimateShiftCoarse：只做降采样 4 倍的全范围粗搜，精度 ±4px，用于逐帧选关键帧
// estimateShift：粗搜后再在全分辨率 ±R 内精搜，只用于 stitch 的关键帧重匹配
// 返回：滚动量 = 匹配到的 y − OFF
```

判据用 **SAD（绝对差和）**，不用归一化互相关——同一段视频的帧是同源的，不存在亮度差，SAD 更快且实测精度相同。

M2 第 1 级把两种精度拆成具名入口：固定 4fps 的相邻帧调用 `estimateShiftCoarse`，
它只负责判断关键帧是否跨过 0.6 屏；`stitch` 仍调用 `estimateShift` 做两级像素级定位。
这样逐帧路径不再为选帧支付全分辨率精搜成本；`stitch` 的定位仍保持像素级，
最终准确率继续由两段 Node 基准和真机评分判定。

⚠️ **直接匹配相隔较远的两个关键帧，不要逐帧累积。**
逐帧累积每帧半像素误差，22 帧就漂 3px；一次性匹配没有累积误差。实测两者拼出的图跑同一套 OCR，一次性匹配的假消息更少。

### 3. 帧选择 —— 固定间隔全取，逐帧位移后选关键帧

按固定 fps（4）全取帧，逐帧 `estimateShiftCoarse`，再由 `pickKeyframes` 选出
"相邻间隔约 60% 视口"的关键帧。**不做试探取帧**——详见下方「取帧策略」节，
那里有 Alice 的产品判断和试探法三次失败的记录。

实测：微信 108 帧 → 6 关键帧 → 长图 720×5107（与逐帧全量基线一致）；
小红书 128 帧 → 18 关键帧 → 720×11738（与基线 11737 差 1px；此为跳转防护前的值，防护上线后现行输出 13558，见 4.5 节）。

M2 第 1 级 Node 回归：微信仍为 6 关键帧、长图 720×5106（基准 -1px）；
小红书仍为 18 关键帧、720×13558。真机耗时与准确率等待 Alice 复测，不在 Node 结果上推断。

⚠️ **回弹帧必须真实排除，不能只数个数。**
滑到底部的橡皮筋回弹会产生负位移。两个层面都要处理，缺一个就会同位置覆盖（"真真假假"重影事故）：
- `pickKeyframes`：负位移的帧从候选里排除（候选落在回弹帧上就往前找最近的实帧，末尾强制帧同理）；
- `stitch`：关键帧间一次性匹配出 `dy ≤ 0` 的帧跳过不贴，继续以上一个已贴帧为参照匹配下一帧。

早期版本只在 `pickKeyframes` 里数了个 `dropped` 计数、什么都没排除，微信素材上结果碰巧正确
（回弹帧的累积位移与前帧相同、恰好不触发选帧），由 Codex 冷读发现计数与行为不符。
修复后小红书长图从 12003 收敛到 11738（基线 11737），**之前 266px 的重影正是负位移帧
同位置覆盖产生的**——OCR 重复组从 12 降到 2。

### 4. 拼接

画布高 = 累积位移 + 可视区高。按累积位移逐帧覆盖贴上，后帧覆盖前帧。

### 4.5 页面跳转防护（stitch 内置）

**定位是兜底，不是支持跳转。** 适用范围不变——界面上仍然告诉用户"中途不要切页面"；
这层防护管的是用户没照做时**不让一次跳转毁掉整份输出**（优雅降级）；跳转瞬间的内容不修复、如实告警。

**`stitch()` 返回的 `warnings` 必须展示给用户**（M1 就要，不是 M2）——产品承诺是忠实还原，
静默降级等于输出可能有缺口而用户不知道；看到"第 N 段疑似切了页面"，用户才能决定要不要重录。
展示形式不限（结果区顶部一行黄字即可），不展示不算完成。

素材中途切页面（点开评论、跳转笔记）会产生"内容整体切换"的帧对——两侧不存在真实位移，
匹配结果是垃圾值。`stitch` 对一次性匹配 `conf < 0.5` 的帧**按 0.95×视口保守推进**并告警，
而不是跳过（跳过会让 anchor 卡死在跳转前、后面全丢——Minis 实测卡死让长图 13861 只剩 7657）。

实测：小红书（9.8–10.8s 有一次跳转）长图 11738 → 13558，去重后 199 行 / 2513 字
（基线 183 行 / 2489 字，多的 1% 是保守推进的边缘重复）。

**语义精确一点**："保住"和"该丢还是丢"各指一半——
- **跳转之后的所有内容**：保住（anchor 不再卡死，链继续，这是防护的全部目的）
- **跳转那一瞬间闪过的内容**：该丢还是丢，防护不试图修复它，只如实告警

微信（无跳转）完全不受影响。

### 5. OCR

分段 **1400px + 重叠 200px**，不要整张长图一次识别——实测整图 62 块 vs 分段 73 块，整图有精度损失。

分段结果**按位置去重，不要按文本相等去重**：跨段 OCR 结果常不一致（同一行一段识别出"是的"、另一段只出"是"），按文本比对会漏掉，然后被后续合并逻辑拼成"是的是"。
规则：`|Δy| < 20 且 |Δx| < 40` 视为同一行，保留文本更长的那次。

### 6. 输出

- **微信模式**：采文字块所在气泡的底色（取块内亮度 >150 像素的中位 RGB）；`G − B > 40` → 我方（微信绿），纯白 → 对方。同气泡续行合并条件：同一侧 + `0 ≤ Δy < 62` + `|Δx| < 60`。
- **通用模式**：按 y 排序，`|Δy| < 18` 视为同一行、按 x 拼接，直接输出。不做任何字段判断。

## 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 客户端 | Expo RN SDK 57 | 复用脉络项目已跑通的栈 |
| 选视频 | `expo-image-picker` | 标准 API，用户点一下授权 |
| 抽帧 | `expo-video-thumbnails` | 按时间点抽帧；⚠️ 一次一帧，需多次调用 |
| 位移+拼接 | **纯 JS，无原生模块** | L3 真机归因：位移阶段 142.5s，其中 JPEG 文件读取与 JS 解码占 71.2%、粗搜占 15.0%；L4 先降 JPEG quality，再依次试预扫 quality、fps 与 UI 让出频率 |
| OCR | `@react-native-ml-kit/text-recognition` | 安卓 ML Kit，离线免费中文；⚠️ 需 dev build，不能用 Expo Go |
| 出包 | EAS 云构建 APK | 本机无 Xcode，安卓走 EAS（凭证不入仓） |

**不需要**：后端、服务器、API key、订阅、账号、云存储、任何联网能力。

## 里程碑

- **M1（当前批）**：能跑通的最小 APK。选视频 → 按 `PRESETS[app].preScan` 预扫定滚动区 → 固定 4fps 全取帧 → 逐帧位移 → 选关键帧 → 拼接 → OCR → Markdown → 复制到剪贴板，显示每步耗时。**含通用 / 微信两种输出模式**（验收要验发言人 95.1%，没有微信模式就验不了）。不做界面美化、不做 iOS、不做多 app 专用规则。
- **M2（当前批）**：**只做性能，不加功能。** 真机微信段总耗时 247s → ≤60s，其中位移**阶段** 219s → ≤30s。L3 的 13 项计时已完成归因，现按「M2 进度」节的 L4 顺序逐项实施并单独验收。保存为文件 / 分享等功能项**移出 M2**，另行排期。
- **M3**：iOS（Vision OCR）+ 界面。**iOS 侧已提前验证完毕、没有未知数**（2026-08-28，
  Minis 在 iPhone 真机 / iSH 用同规格独立实现，同一段素材同一份真值）：
  - **发言人 100%（39/39）与字符级 99.5%** ——⚠️ **已查实：这不比 Mac 侧强，是同一个成绩换了把尺子**
    （2026-08-28，Alice 确认 Minis 用的是同样两段视频，交叉验证得以成立）。
    iOS 用的分母是 39 = 44 − 5，把全部含表情标记的条目都排除了；Mac / Android 侧分母 41 = 44 − 3，
    漏排了两条连发表情。把 Mac 侧换算到同一口径：**拼接架构 99.6% / 39/39，与 iOS 的 99.5% / 39/39 吻合**。
    原先写的「差距是 OCR 引擎不是流水线」是错的归因：引擎差距真实存在
    （同口径下 Vision 99.6% vs ML Kit 96.2%，差 3.4 点），但它解释不了 iOS 99.5% 与 Mac 93.7% 之间那 5.8 点——
    **那 5.8 点全部来自口径**。**Vision 也读不到 `[Facepalm]` 这类图形表情，这跟引擎强弱无关。**
  - 微信长图 720×5107 与 Mac 侧一像素不差；滚动区三次互证（差 0–1px）

> ⚠️ 本节 2026-08-28 内改过三版，方向反转过一次，看代码疑惑时按这条时间线理解：
> ①最初写"固定抽 12 帧"→ ②实测稀疏抽帧翻车后改成 `walkKeyframes` 试探取帧（该方案后已废弃）→
> ③试探法在变速素材上找不到落点（三次修复失败），Alice 拍板"滚动快=不重要，取就行反正能查重"，
> **最终回到固定间隔、但按 4fps 密集取**（108 帧那种，不是 12 帧那种稀疏取）。现行 = ③。

## 已证伪（别再走回头路）

**均匀"稀疏"抽帧不可用（8–12 帧那种）；密集抽帧（4fps 全取）没有这个问题，正是现行主路径。**
曾经的方案是"按固定间隔只抽 N 帧，算位移后丢弃冗余帧"，实测三种失败：

| 抽帧数 | 结果 |
|---|---|
| 8 | 滚动区检测**完全失败**（y154~1522 被误判成 y761~1385，帧太少时跨帧方差不可靠） |
| 12 | 长图 5592 vs 正确 5107，**多出 485px 重影**——稀疏帧跨过回弹段，匹配到错误位置，总位移被高估 |
| 16 | 正确，746ms |
| 108（全量） | 正确，4692ms |

稀疏抽帧的失败根因是**帧与帧之间跨过了回弹段**、位移被高估；4fps 密集抽帧逐帧算位移，
回弹被逐帧识别并真实排除，不存在这个问题。（中途试过 `walkKeyframes` 试探取帧来省抽帧成本，
匀速素材可行、变速素材失败，**已废弃**，见下方「取帧策略」节。）

**位移上限 0.65 是实测定的**，同一实现在不同位移下的准确性：

| 位移占视口 | 误差 | conf |
|---|---|---|
| ≤70% | **0px** | 1.000 |
| 75% | -18px | 0.772 |
| ≥80% | **-714px（完全翻车）** | 0.69-0.78 |

好消息是 conf 在翻车时会掉到 0.85 以下；固定间隔主路径的相邻帧位移远小于这个边界，天然安全。
（Minis 在 iSH 侧用 `apple-vision overlap` 测到同类边界，它那边更早——62.5% 就翻车，
且 **confidence 恒为 1.0 完全不报警**，所以那条链路必须靠位移合理性做兜底，不能信 conf。）

## 中间盘图片格式：用 BMP，不要 JPEG

同一张长图送同一个 OCR：

| 格式 | OCR 块数 | 总字符 | 编码耗时 |
|---|---|---|---|
| PNG（无损） | 71 | 661 | 慢（zlib） |
| **BMP（无损）** | **71** | **661** | **11ms** |
| JPEG q92 | 69 | 655 | 44ms |
| JPEG q88 | 69 | 654 | 44ms |

**诚实说明**：JPEG 少认的那 2 块在这段素材上恰好不影响最终准确率（93.9% vs 93.7%，
在 93.7–94.0% 的正常抖动区间内）。**选无损是出于原理——不引入不可预测的失败模式——
而不是出于实测收益。** BMP 编码比 JPEG 还快，代价只是中间文件大（10MB vs 0.5MB），临时文件无所谓。

`src/stitcher.js` 的 `encodeBMP()` 已实现（54 字节头 + 倒序写像素，24bpp，4 字节行对齐）。
Minis 在 iSH 侧独立测到同样现象，并额外测出 PNG 编码在模拟层比 BMP 慢 53 倍。

## 取帧策略：固定间隔，不做试探

**主路径就是"按固定 fps 全取帧 + 逐帧位移 + 选关键帧拼接"。** 不要试探、不要自适应步长。

这条是 Alice 定的，理由比"哪种算法更准"更根本：

> 滚动快 = 用户认为那段不重要（快速划过正文），滚动慢 = 重要（评论区）。
> 所以不需要精确控制每帧位移落在哪个区间 —— 快滑段丢一点正是用户想丢的，
> 慢滑段重叠多也无所谓，反正能查重。

**配套的设计原则：拼接层宁可多不可少 —— 重复能去掉，丢失补不回来。**
所以位移估计取保守值、宁可多贴一帧，重叠部分交给输出层的 `dedupeLines()` 收拾。

### 实测（两段素材都完整覆盖）

| 素材 | 帧数 | 关键帧 | 长图 | 总耗时 |
|---|---|---|---|---|
| 微信 27s | 108 (4fps) | 6（丢弃回弹 4） | 720×5107 | 4.8 s |
| 小红书 32s | 128 (4fps) | 18 | 720×13558（含 4.5 节跳转防护展开的保守段） | 6.0 s |

抽帧+解码在 **Mac 上**占 55–60%（微信 2.6s/108 帧），是 Mac 侧大头；**真机上大头完全不同**——位移阶段约 219s、抽帧只要 10.7s。L3 后已用六项互斥计时查明：真机位移阶段里，
JPEG 文件读取与 JS 解码占 71.2%、粗搜占 15.0%，其余见「M2 进度」节。
**M2 的优化方向：先归因，再优化；归因已完成，当前按 L4 顺序逐项实测。**
⚠️ 原写「减少位移计算量，不是减少解码」——**这个方向判断没有依据**，已订正：
jpeg-js 解码正是本批明确要测的头号嫌疑项（纯 JS 解 108 帧）。
⚠️ `expo-video-thumbnails` 57.0.1 无尺寸参数，**必须先完整解码**，
所以"抽小图省解码"这条路不存在；可行的是解码之后在 JS 内降采样再进位移链路（五级路线第 3 级）。

### `walkKeyframes` 已废弃（真机数据宣判，不要在任何批次复活）

**账很简单**：它优化的是抽帧次数，而真机实测抽帧只占 10.7s / 247s = **4.3%**——
即使抽帧降到 0 也只省 4%，而它带一个三次修复失败的已知缺陷。收益 4%、风险高，不做。

以下失败记录保留作反例。试探取帧能把抽帧次数从 108 降到 13（微信实测 1.2s vs 4.8s），但它有一个未解的失败模式：
**"前段静止后段猛滑"的素材找不到落点**。小红书那段前面在看正文几乎不滑、后面猛滑，
从 t=0 出发无论步长取多大，位移要么落在静止段（远小于下限）、要么一步跨进快滑段（远大于上限），
60 次探测全耗在震荡上，只收下 1 帧。

三次修复尝试全部失败，记下来防止后来者重走：

| 尝试 | 结果 |
|---|---|
| 按实测速度反推步长 | 微信仍对（13 次），小红书从 4 帧退到 1 帧 |
| 放宽接受区间到"只要 dy>2 就收下" | **连微信也退化**（13 次 → 60 次耗尽，只覆盖 9.0s）。小位移会把速度估计带偏，后面每步反推都跟着错 |
| 加大 maxProbes | 没试 —— 震荡不收敛，加大只是多烧抽帧 |

要重做的话方向是「区间二分 + 允许小位移帧照收但不用它更新速度估计」，**两者必须一起改**，
只改一个已经验证过会翻车。

## ✅ M2 收尾（2026-08-28）

**成果：真机总耗时 246,975 ms → 161,649 ms，降 34.5%。准确率全程未动**
（字符级 90.6% / 发言人 39–41 / 顺序 100%，每一级都与前一级做过逐字节比对）。

| 级 | 内容 | 真机收益 | 结论 |
|---|---|---:|---|
| L1 | 逐帧位移改粗搜-only | −27.7%（位移阶段）| ✅ 保留，输出逐字节一致 |
| L2 | DS 4→8 + stride | — | ⏸ 暂缓（精度风险，收益未测）|
| L3 | 先裁再降 + 13 项细分计时 | −16.2%（位移阶段）| ✅ 保留。**真正价值是那 13 项计时**，它一次定位了归因 |
| L4.1/4.2 | 抽帧 quality 1→0.4 | **−1.6%（总耗时）** | ✅ 保留（无害），但**收益远低于预期** |
| L4.3 | fps 4→2 | 预估 −41% | ❌ **不做** |

### L4.3 为什么不做（有账）

- **收益**：161.6 s → 约 96 s
- **成本**：一天工时 + 一轮 EAS 构建 + Alice 亲手真机测两段 + 逐字节比对
- **风险**：fps 减半是钝器，削覆盖密度，可能丢内容
- **决定性理由**：它优化的正是 M3 大概率要**整个删掉**的那条路径
  （像素对齐链路 128.6 s = 解码 96.5 + SAD 25.2 + 拼接 6.9）。
  M3 若成立，96 s 这个中间态就没有意义。

### M2 留下的两条最值钱的经验

1. **"quality 降低 = 解码变快"是错的。** JPEG 解码分三步：熵解码（∝文件大小）、
   反量化/IDCT、色彩转换（后两步 ∝ **像素数**）。quality 只压第一步，
   而像素数没变（720×1652），所以只降 4.9%。
   **一度以为降了 20.8%，那个读数来自一次屏幕熄灭的污染测量。**
2. **安卓后台节流会让 `setTimeout(0)` 变成 5.8 秒。** 屏幕一暗，UI 让出从
   307 ms 暴涨到 156,040 ms（508 倍），总耗时从 161 s 变 297 s。
   **任何在真机上跑长任务的测量，都必须记录"屏幕是否常亮"这个条件**，
   否则数据不可比。这条差点让我们把一个假结论写进图纸。


## 🔴 架构级发现：直接 OCR 可能比拼接快 10 倍（2026-08-28，Alice 提问触发）

**Alice 的原话**：「为什么要走这个拼接呢？不是直接 OCR 吗？」

**先承认一件事：这条路我从没量化对比过。** 最早设计时就选了拼接，理由写在图纸里
（重叠去重、边界截断、排序在像素层更干净），但**从来没实测过"直接对每帧 OCR"到底行不行**。
被问到才去测，测出来的结果如下。

### 实验：15 帧直接 OCR，三种去重策略

| 方案 | 字符级 | 发言人 | 条数（真值 41）|
|---|---:|---:|---:|
| 拼接（当前架构，Mac） | 93.7% | 95.1% | 41 |
| 直接 OCR + 相似度去重 | 93.0% | 92.7% | 48 |
| 直接 OCR + 纯几何去重 | 73.8% | 73.2% | 34（**丢内容**）|
| **直接 OCR + 文本锚点 + 几何限域 + 文本匹配** | **93.9%** | **95.1%** | 48 |

**最后一种追平了拼接**，多出的 7 条全是 OCR 噪声碎片（`5。` `品品` `00` `2ナ日`），
长度过滤即可，不是架构问题。

### 关键手法：用 OCR 文本当锚点算位移，完全不解码像素

相邻帧取共同的长文本行（归一化后完全相同、长度 ≥4），它们的 **y 坐标差取众数**就是帧间位移。
实测每对帧有 **6–17 个共同锚点**，位移众数明确；**最后一对测出 −76px，正是回弹**，
与像素法（SAD）测得的完全一致。

**这意味着 jpeg-js 解码和像素位移估计都可以整个砍掉。**

### 为什么这可能快 10 倍

当前真机 161.6 s 里，**jpeg-js 解码 96.5 s + 逐帧粗搜 25.2 s + 拼接 6.9 s = 128.6 s（80%）
全部是为了做像素级对齐**。而直接 OCR 路线：

- ML Kit 原生读文件，**不需要 JS 解码**（jpeg-js 那 96.5 s 直接归零）
- 位移由文本锚点给出，**不需要 SAD**（25.2 s 归零）
- 不需要拼接（6.9 s 归零）
- 代价：OCR 从 4 段变成 15 帧。真机 OCR 单段约 842 ms → 15 帧约 12.6 s

**粗算真机 15–20 s，对比现在 161.6 s。**

### 但先别推翻 M2 —— 这条路的验证还没走完

1. 只在**微信单聊**上测过；小红书评论区结构更复杂，锚点密度和去重难度都未知
2. 用的是 macOS Vision，**不是真机 ML Kit**；OCR 引擎不同，锚点匹配率可能变化
3. 「15 帧」是拍的，覆盖保证靠的是重叠冗余，没有 `ok` 那样的失败自检
4. 拼接方案的失败模式是**几何的、可预测的**；文本锚点方案的失败模式是**文本的**——
   锚点不足时会静默降级，而这正是我们一路在防的那类问题

**定位：这是 M3 的一条独立路线，不是 M2 的补丁。** M2 按原计划收尾（拼接方案已验证可用），
M3 单独立项验证直接 OCR，验证通过再谈替换。

**这条记录本身值得留着**：一个非技术出身的产品负责人问「为什么不直接 OCR」，
比任何一轮代码审查都更接近架构的根子上。


## M2 进度：L1、L3 已验收，L2 暂缓，L4.1 / L4.2 待真机验收

### L1（逐帧位移改粗搜-only）— 有效但不够，2026-08-28 真机验收

| | M1 | M2-L1 | 变化 |
|---|---|---|---|
| 位移计算（取帧与位移 − 抽帧） | 218.8 s | **158.2 s** | **−27.7%** |
| 总耗时 | 247.0 s | **191.0 s** | −22.7% |
| 抽帧累计 | 10.7 s | 10.2 s | 不变（未动） |
| 字符级 / 发言人 | 90.6% / 39–41 | **90.6% / 39–41** | **输出逐字节完全一致** |

**精度结论**：粗搜 ±4px 对最终输出**零影响**——不是"在容差内"，是与 M1 真机输出
一个字符都没差。长图 5117、7 关键帧也与 M1 真机一致。L1 保留。

（另：上一轮真机曾报"单帧抽取 14754ms"触发 2000ms 停止线，重跑后抽帧 10.2s 正常，
确认是设备热降频等偶发状态，非代码问题。那道停止线加得对——没有它，app 会闷头跑
二十多分钟再给出一个误导性的耗时数字。）

### L2 暂缓（因精度风险，不是因为收益被否定）

⚠️ **本节 2026-08-28 订正过，原写"收益已被数据否定"是错的**，依据算错了，见下方 L3 节的订正说明。

L2（粗搜 DS 4→8 + stride=2）暂缓的**唯一成立理由是精度风险**：它会把粗搜精度从 ±4px
降到 ±8px，而 L1 那个"输出逐字节完全一致"的结论是在 ±4px 下取得的，降精度后必须重验。
L3 真机归因显示粗搜占位移阶段 15.0%；L2 仍只因 ±8px 的精度风险暂缓，当前不实施。

### L3（已验收）：预计算小灰图

**先订正一个我算错的依据。** 原文写"`downsample()` 每次遍历约 98 万像素"，
**错了**——它是跳采样，只循环 `dw×dh = 180×342 = 61,560` 次
（见 `stitcher.js` 的 `downsample`），不是全图遍历。由 Codex 冷读时查实。

按真实循环量重算 108 帧的总计算量：

| 操作 | 单次 | 108 帧合计 |
|---|---:|---:|
| `toGray()` | 984,960 次/帧 | 1.06 亿 |
| `downsample()` ×2 | 61,560 次/调用 | 0.13 亿 |
| coarse SAD | **3,618,000** 次/帧对（268×180×75，搜索位置是闭区间 `dh−dBAND+1`，我先前少算了 1 个）| 3.87 亿 |
| 精搜 SAD（M1 有，L1 已省掉） | 10,584,000 次/帧对 | 11.32 亿 |

**由此得出一个必须正视的事实**：L1 省掉了 11.32/16.38 = **69% 的 JS 循环量，
真机却只快了 27.7%**。差的这 41 个百分点说明——**位移阶段的时间大部分不在这三个循环里**。

「取帧与位移」减去「抽帧累计」之后，剩下的仍然包含：
**jpeg-js 解码（纯 JS，最可疑）、关键帧二次解码与复制、UI pause、GC**。
现有数据**不足以**判断主要耗时归谁。

**⚠️ 实施方式：先裁再降，不是「整帧先降采样」。**

先 `cropRows` 零复制裁出滚动区，再用**融合的 `prepareCoarseGray()`** 一遍完成
「灰度化 + 1/4 跳采样」；逐帧位移全在小灰图上跑，只有 `stitch` 的关键帧重匹配（6–7 对）用全分辨率。

**为什么不能先降再裁**：L1 的实际采样行是 `region.top + 4k`。实测微信 `top=154`、
小红书 `top=182`，**两者 `top % 4` 都等于 2**；若按整帧 `y=0` 先降采样，采样行变成 `4k`
（156/160/164…），与 L1 整体**错开 2px**，「输出逐字节一致」这条验收标准会直接失效。
由 Codex 冷读时查实。

**所以 L3 的定位要如实说**：它省掉的是全分辨率 `toGray`（1.06 亿）与重复 downsample（0.13 亿），
合计约占已知 JS 循环量的 24%；**coarse SAD 的 180×342 搜索网格不变**
（它本来就在 1/4 边长的图上跑）。原交接材料写的"后续所有运算 /16"**不成立**，已订正。
后续真机验收结果见下：输出逐字节一致，总耗时相对 M1 降 33.5%。

### ✅ L3 真机验收通过（2026-08-28，OPPO / ML Kit）

| 项 | M1 | L1 | **L3** |
|---|---:|---:|---:|
| 总耗时 | 246,975 ms | 191,028 ms | **164,261 ms**（M1→L3 **−33.5%**）|
| 位移阶段（除抽帧） | 218,818 ms | 158,246 ms | **132,619 ms** |
| 字符级 / 发言人 | 90.6% / 39–41 | 90.6% / 39–41 | **90.6% / 39–41** |
| 与 L1 输出比对 | — | — | **逐字节完全一致** |
| 小红书（飞行模式） | — | — | **整段跑通，长图 11738 与 M1 真机一致** |

**未分类差额两段都只有 6 ms** —— 13 项计时对账是准的，归因数据可信。

### 🔴 真机归因：解码 71%，且「只打解码不够」

| 项 | 微信段 | 占位移阶段 |
|---|---:|---:|
| **JPEG 文件读取 + `jpeg-js` 解码** | **101,483 ms** | **71.2%** |
| 逐帧粗搜（SAD） | 21,410 ms | 15.0% |
| 位移阶段抽帧 | 9,863 ms | 6.9% |
| 关键帧准备 | 6,028 ms | 4.2% |
| UI 让出 | 3,196 ms | 2.2% |
| 小灰图（L3 优化的对象） | 496 ms | **0.3%** |

**必须正视的一条推算**：**即使把 JPEG 文件读取与 JS 解码时间归零，总耗时也只降到 62.8 s，仍超 60 s 目标。**
所以 L4 需要依次试多项手段；每项独立提交、独立归因，不能把帧数变化混进 quality 实验。

**三个新发现（微信段单看不明显，两段对照才浮出来）**：

1. **逐帧解码单价稳定在 866–940 ms/帧**（两段一致），是 jpeg-js 在 Hermes 上的固定成本。
2. **预扫单帧比逐帧还贵**（1142–1166 ms/帧）—— L3 当时的预扫同样用 `quality: 1` 全质量解码，
   而它只服务于「逐行跨帧标准差」这一个粗判断，**完全不需要高质量**。
   微信预扫 13.7 s、小红书 28.0 s，这块此前从没被当作优化对象。
3. **UI 让出的单次代价会飙升**：微信 118 ms/次，**小红书 579 ms/次（贵 5 倍）**。
   代码是每 4 帧 `await pauseForUi()` 一次；长图更大、内存压力更高时单次代价暴涨，
   小红书这一项吃掉 18.5 s。


### 🔴 L3 归因结果：JPEG 读取与解码占 81%，我的假设错了（2026-08-28 Mac 侧实测）

细分计时一次就回答了归因问题，答案与假设相反：

| 项 | Mac 耗时 | 占位移阶段 |
|---|---:|---:|
| **`decodeMs`（JPEG 文件读取 + `jpeg-js` 解码）** | **2494 ms** | **81.4%** |
| `shiftMs`（coarse SAD） | 397 ms | 13.0% |
| `keyframeMs` | 145 ms | 4.7% |
| `grayMs`（小灰图生成） | 29 ms | 0.9% |
| 未分类差额 | **0 ms** | 对账完美 |

**我做 L3 的理由是「`toGray` 与 downsample 是大头」——实测它们合计 0.9%。**
真正的大头是 **JPEG 文件读取与 `jpeg-js` 解码，81.4%**，而 L3 一点没动它。
Codex 从第一轮就在提醒"位移阶段还含 jpeg-js 解码"，我一直当成次要项。

L3 仍有价值（Mac 侧位移阶段 4362→3065ms，降 30%），但**它优化的是 13% 那一块里的一部分**，
天花板注定很低。**下一级必须打解码。**

### L4（当前批）：按四项顺序逐级实测

1. **L4.1 主阶段降低 quality（已实现，待真机）**：固定 4fps 的逐帧粗搜使用 `quality: 0.4`；选中关键帧后，
   按同一时间点另行以 `quality: 1` 重抽，保证全分辨率拼接与 OCR 输入不使用低质量 JPEG。
   Android 实现仍会先取完整 Bitmap，quality 只控制随后写出的 JPEG 压缩质量，收益**待真机实测**，
   不能预先按文件大小推算。风险是压缩噪声改变粗搜与选帧。
2. **L4.2 预扫降低 quality（已实现，待真机）**：预扫只服务逐行跨帧标准差，改为 `quality: 0.4`。
   它必须单独提交，并用 `prescan` 阶段耗时归因；滚动区坐标若变化，必须单独解释。
3. **L4.3 fps 4→2**：帧数减半，同时削弱覆盖密度。必须单独提交，并验微信
   5117±5、小红书 11738±5 以及准确率。
4. **L4.4 降低 UI 让出频率**：每 4 帧改为每 8 或 16 帧，让用户等待时的界面响应与耗时一起评估。

前三项都会改变解码出的像素或取帧密度，必须各自独立 commit、分别报告真机收益，并与
L3 真机输出逐字节比对；若文本有差异，再用 `score.py` 验字符级 ≥90%、发言人 ≥39/41。
L4.1 与 L4.2 完成后先做一次真机回传，不提前实施 L4.3。L5 原生/JSI 动工前必须停下来问。


### L3 同时完成的细分计时（已通过真机对账）

L3 的代码改动里**一并加入了位移阶段的细分计时**，回答了 L3 值不值、剩余时间花在哪两个问题。
**六项必须互斥、可相加**（否则总和对不上、归因失效）：

| 计时项 | 覆盖 |
|---|---|
| `decodeMs` | 4fps 首次逐帧 JPEG 解码。**排除**关键帧二次解码 |
| `grayMs` | 首次逐帧生成小灰图。**排除** stitch 阶段的 toGray |
| `shiftMs` | 预计算小灰图上的粗搜。**不含** downsample（L3 后逐帧粗搜不再内部降采样）|
| `keyframeMs` | `pickKeyframes` + 关键帧二次解码与复制。**排除** pause |
| `pauseMs` | 位移阶段显式 `pauseForUi()` 的等待 |
| `shiftThumbMs` | **位移阶段**的 `getThumbnailAsync`（新增，见下）|

⚠️ 原有的 `thumbnail` 一行累计的是「预扫 + 4fps 主阶段」两段之和，**不能直接代入对账**，
所以单列 `shiftThumbMs`。面板共 **13 行**（原 7 + 新 6）。

**对账要求**：内部算未分类差额
`frameShift − (shiftThumbMs + decodeMs + grayMs + shiftMs + keyframeMs + pauseMs)`，
超过 `max(100ms, frameShift × 1%)` 就在面板上显式告警。
**差额本身就是信息**——它意味着还有一块没想到的开销（进度上报、数组分配、GC…），
而那正是下一级该优化的东西。没有告警，差额会被静默吸收掉。

**合并做而不是分两批的理由**：加计时点的代码成本近乎为零，而每多一批就要多一次
EAS 构建 + Alice 亲手真机测试。一次构建同时拿到收益与归因，成本等于只做 L3。

## M1 真机验收结果（2026-08-28，OPPO Find N6 / ML Kit，Alice 亲测）

三项硬验收全过：

| 项 | 真机结果 | 验收线 |
|---|---|---|
| 字符级准确率 | **90.6%** | ≥90% ✓（Mac 基准 93.7%，3 个点差距全在 OCR 引擎：ML Kit 错字率高于 macOS Vision） |
| 发言人 | **39/41 = 95.1%** | ≥39/41 ✓（与 Mac 完全一致——该判据只依赖颜色，跨引擎稳定，三平台三连 95.1%/100%/95.1%） |
| 顺序 | 100% ✓ | — |
| 飞行模式 | 小红书整段在飞行模式下跑通 ✓ | APK 本身无 INTERNET 权限 |
| 跳转告警 | 微信段无告警（正确：无跳转） | — |

真机与 Mac 的已解释差异：微信 7 关键帧/长图 5117（Mac 6/5107）、小红书 17 关键帧/11738
（Mac 18/13558，真机选帧路径恰好跨过跳转段且匹配成功、防护未触发）——
`expo-video-thumbnails` 与 ffmpeg 的抽帧时间点不完全一致所致，内容准确率过线说明无实质影响。

### 🔴 最重要的真机发现：瓶颈不是抽帧，是位移计算

总耗时 **247 秒（微信）/ 278 秒（小红书）**，分解后真相与预判相反：

| 阶段 | 真机实测 | 之前的预判 |
|---|---|---|
| 抽帧累计 | **10.7 s** | "最大风险，可能 40s" ← **押错了** |
| 位移**阶段**（取帧与位移 − 抽帧） | **约 219 s** | Mac 上只要 1.7s，没当回事 ← 瓶颈在此**阶段**，具体归谁未定 |
| OCR | 2.8 s | — |

真机位移**阶段**约 2s/帧对（Mac 上仅 8–15ms）。⚠️ 该阶段含 SAD、jpeg-js 解码、关键帧二次解码、pause、GC，**这 2s 内部归谁未定**——L3 的 13 项细分计时就是为了回答它。

**M2 第一优先级：把位移计算压到 30s 内。**
⚠️ 此处原写"`getThumbnailAsync` 可指定宽度抽小图"——**这个前提是错的**，
Codex 查实 `expo-video-thumbnails` 57.0.1 的 options 只有 `quality/time/headers`，没有尺寸参数
（又一例"凭印象写文档"）。真实可行的候选手段，按成本从低到高给 M2 试，
**每步都用 score.py ≥90% / 发言人 ≥39/41 回归护住**：

1. **逐帧位移只做粗搜**（DS=4 精度 ±4px 对"选关键帧"这个粗判断足够）；
   全两级精搜只留给关键帧间的 stitch 重匹配（一段视频只有 6–17 对）。
2. 粗搜 DS 4 → 8，SAD 加 stride=2 隔行采样（`sad()` 已有该参数）。
3. **先裁再降**：`cropRows` 零复制裁出滚动区 → 融合的 `prepareCoarseGray()` 一遍完成
   灰度化 + 1/4 跳采样，省去全分辨率 `toGray` 与重复 downsample，coarse SAD 网格不变。
   ⚠️ **不能整帧先降采样**：L1 采样行是 `top+4k`，实测两段素材 `top%4` 都等于 2，
   整帧从 y=0 降会错开 2px，破坏「输出逐字节一致」的验收标准。详见「M2 进度」节。
4. 抽帧 fps 4 → 2（107 对 → 53 对），仅当上面不够时用——它同时削覆盖密度。
5. 以上全不够才考虑 JSI/原生——**先证明纯 JS 到不了 30s 再动这张牌**。

## 已知未解风险（M2 要回答的）

2. ~~ML Kit 在长图上的表现未验证~~ **已验证**（真机 720×11738 分段 OCR 正常，字符级 90.6%）。
3. ~~内存要实测~~ **已验证**（真机完整跑通 720×11738，无 OOM）。

## 🔴 M3 实测天花板：纯文本框坐标判不出满宽消息的发言人（2026-08-28）

**这是信息层面的结论，不是实现质量问题。** 微信的左右气泡布局左右对称
（左 134px 头像区、右 135px 头像区），一条消息只要长到占满气泡宽度，
它的文本框坐标就与对侧的满宽消息完全一致。

实测微信段 16 帧：**13 条双贴峰行的几何参数一模一样 —— `x=134 右=585 w=451`**，
而它们分属不同发言人。任何只读 `x/y/w/h` 的判据在这里都没有可用信息。

### 拆开看 M3 第二轮（Codex 提交 `4895ba4`）的 39/41

| 版本 | 字符级 | 发言人 |
|---|---:|---:|
| 完整交付 | 93.9% | 39/41 = 95.1% |
| 去掉「孤立回复先验」 | 93.9% | 38/41 |
| 再去掉「中点二分传播」 | 93.9% | **35/41 = 85.4%** |

- **架构裸能力 = 35/41（85.4%）**，这是可迁移的部分
- 两条启发式合计贡献 4 条：「中点二分传播」+3、「孤立回复先验」+1
- 两条都是从这 41 条数据倒推的，没有 UI 结构依据。
  「中点二分」假设说话人在两个明确气泡的中点切换 —— 换一段聊天可能全错
- **字符级 93.9% 在三个变体里一个点都没动**，说明字符级达标不含先验水分

**结论：报数字要报 85.4%，不要报 95.1%。** 两条启发式都不该进 `src/`。

### 三条出路（等拍板）

1. 接受 85.4% —— 但低于交接文档的验收线 ≥39/41
2. **只对歧义气泡采样像素**：13 个满宽气泡各采 1 点判底色。
   Android 侧用 `BitmapRegionDecoder` 只解码小块，不需要整帧解码，
   相对 jpeg-js 整帧 96.5 秒可忽略。判据变回确定性的（绿色 = 我）。
   代价是"完全不解码像素"这个前提不再成立
3. 放弃发言人标签 —— 产品上不可接受

**没有第四条路可以靠更聪明的纯坐标算法绕过去**，上面的 13 条数据就是证明。


## 🔴 尺子的满分是 39/41，不是 41/41（2026-08-28 查实）

`groundtruth.json` 的 41 条分母里，有两条任何 OCR 路线都产不出：

```
[them] [Facepalm][Facepalm][Facepalm][Facepalm][Facepalm]
[them] 景甜[Lol][Lol][Lol]
```

`score.py` 的排除正则 `^\[(图片|表情|Facepalm|Lol|视频|动画表情)\]+$` 只允许一个左括号，
单个 `[Facepalm]` 排得掉、连发的排不掉，于是留在了分母里。

**三个完全独立的实现都停在同一个数字上，这就是证据**：

| 实现 | 发言人 |
|---|---:|
| 现有拼接架构（Mac / Vision） | 39/41 |
| M2-L3 真机基线（Android / ML Kit） | 39/41 |
| M3 文本锚点（Mac / Vision） | 39/41 |

所以「验收线 ≥39/41」实际含义是**要满分**，而不是「允许错两条」；
「基准 95.1%」也不是「还差 4.9 个点」，它已经顶格。这个误读一路传进了
交接文档的 Done when、以及 README 对外的对比表。

**处置**：有意**不修**那个正则 —— 修了分母会从 41 变 39，M1/M2/M3 的历史数字将全部不可比。
改为让 `score.py` 每次运行时自己打印上限并列出那两条，尺子不动、但不再误导人。

### 换算到同一口径（分母 39）后的横向对比

| 实现 | 字符级 | 发言人 |
|---|---:|---:|
| Mac 拼接架构（Vision） | 99.6% | 39/39 |
| M2-L3 真机（ML Kit） | 96.2% | 39/39 |
| **M3 文本锚点（Vision）** | **99.8%** | **39/39** |
| iOS 独立实现（Vision，Minis 报） | 99.5% | 39/39 |

两条结论：

1. **iOS 那列不是亮点，它和 Mac 拼接架构是同一个成绩。** 三个 Vision 实现全部落在 99.5–99.8%，
   差异在 39 条分母下是噪声。
2. **同口径下 M3 文本锚点的字符级已追平拼接架构**（99.8% vs 99.6%，同样是噪声级差异）。
   这条路线在质量上没有付出代价，省掉的是像素对齐那 80% 的耗时。

**待 Alice 拍板**：`README.md` / `README_CN.md` 的对比表里
「发言人归属 95.1% / 95.1% / **100%**」把 iOS 那列加粗成了亮点，对外会读成 iOS 版更准。
建议改成显式带分母与排除项的写法，例如「39/39（2 条纯表情消息 OCR 无法识别，不计入）」——
既不虚高，也不再让同一个成绩看起来像三个。改不改、怎么改由她定。

## ✅ M3 Mac 侧验收通过（2026-08-28，Codex 提交 `7f4b1a8`，CC 独立复跑核实）

**状态：`PARTIAL_EXEC`** —— Mac 侧五项全过，真机两项未开始。

| 项 | 结果 |
|---|---|
| 微信段（带 `--frames` 局部采样） | 字符级 **93.9%**、发言人 **39/41（满分）**、顺序完全正确、自检 15/15 |
| 13 个歧义气泡 | **全部由局部像素确定判定**（我 7 / 对方 6 / 未定 0） |
| 降级路径（不传 `--frames`） | 35/41，不报错 —— 纯坐标裸能力，符合预期 |
| 小红书段 | 202 行（现有架构 199 行），自检 18/18 通过 |
| 稀疏反例 `stride=12` | 仍报 4 对告警、退出码 2 —— 自检没有被"修好" |
| `stride` 4–10 | **全部 39/41**，字符级 93.7–93.9% |
| 工程 | `npm test` 36 pass / lint / `doc:check` 全绿 |

**两条无结构依据的启发式已删除**（`propagated-nearest-definite`、`single-double-sandwich-fallback`），
它们原先合计贡献的 4 条，现由局部像素采样完整覆盖。
数字没变（都是 39/41），**但成色变了**：从「35 条真本事 + 4 条碰运气」变成「39 条全部确定性判定」。

### 局部采样的实际开销（Mac 侧实测）

只解码 OCR 文本框矩形，不展开整帧：

| 项 | 值 |
|---|---:|
| 解码像素 | 235,734 px |
| 同素材整帧解码量（720×1652×16 帧） | 19,031,040 px |
| **占比** | **1.2%** |
| 13 次 `djpeg -crop` 合计 | 39.6–55.8 ms（多次运行波动，CC 复跑取到 55.8） |

Android 侧设计见 `verify/ANDROID-SAMPLING.md`（`BitmapRegionDecoder` 路线，
保守预算 ≤1 s，占 M3 总耗时目标 40 s 的 ≤2.5%）。**该预算尚未在真机验证。**

### 还差的两条 Done when（都需要真机 + Alice 亲测）

4. APK → 真机跑两段 → 回传新路线全部分项耗时、计时对账 + Markdown，目标总耗时 **≤40 秒**（现 161.6 秒）
5. 与 M2-L3 真机输出逐字节比对（预期有差异，有差异时跑 `score.py` 看质量有没有掉）

**当前进度**：候选路线已经作为 Android App 内可切换的第二条路径实现，包含
「逐帧 ML Kit OCR + BitmapRegionDecoder 局部采样」。EAS preview APK 已从提交 `adc6306`
编译完成，尚待 Alice 真机验收与回传后的逐字节/质量比较。
按交接文档，**拼接架构在真机验证通过前不能删**，当前仍是默认路径。

## 🔴 M3 真机验收：耗时目标大幅超额，质量两处未过（2026-08-28，OPPO / ML Kit，Alice 亲测）

**状态：`PARTIAL_EXEC`。** 架构的性能承诺兑现了，质量还差两处。

### 微信段：耗时 36 倍，字符级差 0.8 个点

| 项 | M3 文本锚点 | M2-L3 拼接 | 判定 |
|---|---:|---:|---|
| **总耗时** | **4,501 ms** | 161,600 ms | ✅ 目标 ≤40 s，**超额 8.9 倍** |
| 字符级 | **89.2%** | 90.6% | ❌ 验收线 ≥90%，差 0.8 点 |
| 发言人 | 39/41（满分） | 39/41（满分） | ✅ |
| 顺序 | 完全正确 | 完全正确 | ✅ |
| 失败自检 | 15/15 通过，无告警 | — | ✅ |
| 歧义气泡 | 13 个全部像素判定（未定 0、错误 0） | — | ✅ |

真机耗时分项（对账通过，delta 1 ms）：

| 阶段 | 耗时 |
|---|---:|
| 选定帧抽取（MediaMetadataRetriever） | 2,598 ms |
| 逐帧 ML Kit OCR（16 帧） | 1,674 ms |
| UI 让出 | 141 ms |
| 固定 UI / 锚点 / 去重 | 11 ms |
| **歧义气泡局部采样（13 块）** | **60 ms** |
| Markdown + 清理 | 17 ms |

**局部采样 60 ms，占总耗时 1.3%** —— `ANDROID-SAMPLING.md` 保守估的 ≤1 s 预算，实际只用了 6%。
解码 172,339 px，10 个 decoder（同帧复用生效）。累积位移 3,740 px，与 Mac 侧 3,744 px 差 4 px。

### 小红书段：不可用

18 对锚点中 **5 对告警**（Mac 侧同 stride 是 0/18）：2 对锚点为 0、3 对仅 2 个。
累积位移 **9,369 px，比 Mac 侧的 10,414 px 少 1,045 px** —— 位移算错导致同一内容
映射到不同全局 y，去重的 ±90 px 邻域抓不住，输出出现大面积重复行
（去重后 301 行 vs Mac 侧 253 行）。

### 根因：文本锚点对 OCR 稳定性的依赖，被 Vision 掩盖了

锚点判据要求相邻帧的文本行**归一化后完全相同**。Vision 识别稳定，Mac 侧因此 0 告警；
ML Kit 抖动大（同一行两帧可能识别出一两个字的差异），锚点直接失配。

这个风险 PLAN 里早就写过（「用的是 macOS Vision，不是真机 ML Kit；OCR 引擎不同，
锚点匹配率可能变化」），**这次是它兑现了**。同源的影响也体现在微信段字符级上：
多处输出是两次识别被拼在一起（`张继科料`、`|要死要是被`、`连续找我两个`），
说明去重「更长的胜出」规则在识别抖动下会选到含噪声的更长版本。

**但失败自检兑现了它的承诺**：5 对告警明确报出，状态为 `warning`，没有静默降级。
这正是本批立项时（交接文档第六节）指认的最大风险，机制有效。

### 下一步：预算极其充裕，两条改进都用得起

现在 4.5 s，目标 40 s，**余量 8.9 倍**。这是这条架构自己挣出来的预算，应该花在质量上：

1. **锚点改模糊匹配** —— 现要求归一化后完全相同，改为相似度 ≥0.9。
   ML Kit 的抖动通常只错一两个字，模糊匹配能捞回大量锚点。这是对症的那一条。
2. **降低 stride 取更多帧** —— 帧间重叠更多、锚点更多。OCR 现在只花 1,674 ms，
   stride 7→3 约涨到 3.8 s，总耗时约 7 s，仍远低于 40 s。
3. **去重的「更长的胜出」要防噪声** —— 微信段字符级那 0.8 个点主要来自这里。

⚠️ **真机测试要 Alice 亲手操作，所以改到位再出下一个 APK**，不要开多轮。

## 🟡 M3 第四轮候选：可导出真机 OCR，模糊锚点与多帧共识待一次真机复验（2026-08-28）

**状态：`PARTIAL_EXEC`。** Mac 回归和 Android 工程实现已完成；新的设备质量、完整 4fps
诊断模式耗时以及三项真实删除测试，要等本批唯一 APK 返回两份 OCR bundle 后才能判定。

### 先解除“每调一次参数就要重新出 APK”

App 的 M3 页面新增“导出完整 4fps OCR JSON”开关，第四轮验收候选默认开启；第五轮性能
候选已改为默认关闭。打开时会对源序列的
108 / 128 帧全部做 ML Kit OCR，再从完整序列中按最终 stride 取子序列处理。流程结束后，
Android 系统目录选择器写出：

```text
tenglu-m3-ocr-模式-时间/
  ocr/f_0001.json ...       # 每帧严格为 text/x/y/w/h/conf 数组
  meta/stats.json           # 算法、耗时、逐对锚点、采样与去重统计
  M3-OCR-BUNDLE.json        # 便于单文件回传的同内容 bundle
```

不导出视频、JPEG 或像素。`verify/import-device-ocr.mjs` 可把 bundle 还原成
`anchor-ocr.mjs` 直接读取的缓存目录。ML Kit 依赖原本漏掉了 Android API 已提供的
`Text.Line.getConfidence()`；安装阶段只对锁定的 `2.0.0` bridge 加这一字段，源码版本或
补丁锚点不符会中止，不填虚假 confidence。

### 模糊锚点不是“相似度过线就算”

相似度采用 `1 - Levenshtein / max(codePointLength)`，阈值选 0.90。精确匹配行为原样保留；
模糊候选必须互为严格唯一最佳。精确锚点已建立可靠位移时，模糊候选还必须落进同一
±3px 簇，且不能改变精确锚点给出的位移。精确锚点不足时，至少 3 个候选、60% 同簇且
最大簇无并列，才允许独立救援。

Mac 两缓存的对照实验：

| 素材 | 判据 | raw / 门禁后 / 最大簇 | 最低簇占比 | 告警 | 累积位移 |
|---|---:|---:|---:|---:|---:|
| 微信 stride7 | exact / 0.95 / 0.90 | 162/162/158（三者相同） | 83.3% | 0/15 | 3,744 |
| 小红书 stride7 | exact | 134 / 134 / 130 | 80% | 0/18 | 10,414 |
| 小红书 stride7 | 0.95 | 135 / 135 / 131 | 80% | 0/18 | 10,414 |
| 小红书 stride7 | **0.90** | **149 / 147 / 143** | **80%** | **0/18** | **10,414** |
| 小红书 stride12 | exact | 33 / 33 / 31 | 0% | 4/10 | 5,487 |
| 小红书 stride12 | 0.90 | 36 / 35 / 33 | 0% | 4/10 | 5,487 |

阈值 0.90 单独使用会误收 `昨天18:12四川 回复` ↔ `昨天18:15四川 回复`：相似度 0.909，
但 y 差 279px，正确簇是 946px。几何门禁拒绝它；stride12 红灯仍是 4 对告警、退出码 2。
0.95 在正常小红书段只多 1 个锚点，0.90 多接纳 13 个且不降低最低簇占比，因此选 0.90。

### stride 来自最大时间间隔，不按 App 写死

⚠️ 第五轮真机横扫已推翻：统一规则是 4fps 下相邻处理帧不超过 750ms、因此 stride=3，
只是第四轮候选；当时两段分别取 36 / 43 帧。
stride 2–7 网格中，stride3 的小红书最少锚点 5、最低簇占比 80%；stride4/5 只有 3–4
个和 60%。stride2 没再提高最少锚点，却把小红书推到 208 行。最终选择不是为追求速度，
而是优先给 ML Kit 抖动留余量；本批不做告警后插帧。

Mac stride3 结果是：微信 93.7%、39/41、顺序正确、35/35 自检、42 行；小红书 202 行、
42/42 自检、累计 10,414px。微信比 stride7 的 93.9% 低 0.2 点，说明更多帧不保证字符
质量；它换来的是更密的重叠和更多锚点，这项取舍必须由新真机数据裁决。

### 去重只在至少三票严格多数且属于同一文本变体时推翻“更长胜出”

同一可靠几何候选簇先按 frameIndex 合并同帧 OCR fragments，每帧最多一票。只有同一归一化
全文至少 3 票且严格过半，并且多数版本与原 `longer-wins` 版本的归一化 Levenshtein
相似度 ≥0.8 时，才采用多数 observation；否则继续用旧版本。第二道文本变体门禁是必要的：
几何候选簇仍可能混入相邻行，仅凭票数会把完全无关的内容互换。输出始终来自一次真实 OCR
observation，不合成新字符，也不按长度过滤消息，所以单字“牛”仍保留。

复核删除测试发现：Mac 小红书 stride3 在没有文本变体门禁时会改选 36 次；加入 0.8 门禁后
改选 28 次、拒绝 8 次，其中 6 次相似度为 0，包含
`展开127条回复 → 喵酱爱喝冰雪碧`、`展开更多回复 → momo` 等确定无关替换。
另外两次相似度为 0.667 / 0.750；小红书没有 groundtruth，不能把它们主观标成对或错。
合成的真实整行一字插入测试（`张继科料也是个渣子`）相似度高于 0.8，仍由三票多数纠正；
完全无关的短消息与单字消息不会仅凭 edit distance 被当成同一文本。

删除测试：Mac 微信 stride7 开启共识与 `--dedupe longer` 都是 93.9%、39/41，Markdown
逐字节相同，净贡献 0；合成的 `[正确, 正确, 正确, 含插入的更长版本]` 测试可证明规则会
拒绝插入噪声，但不能冒充真机贡献。真机第一轮只保存了最终 Markdown，没有逐帧候选框，
所以 `张继科料`、`|要死要是被` 等 6 处插入型问题的真实净增量，必须等 bundle 回来后用
`--dedupe longer` 删除测试量化。

### 第四轮当时仍需真机回答（已由第五轮缓存与真机数据回答）

1. 完整 4fps 诊断开关的总耗时是否仍 ≤40s。按第一轮抽帧/OCR线性估算，微信约 29s，
   小红书约 34–36s；这是估算，不是实测。
2. 微信字符级是否从 89.2% 回到 ≥90%，发言人是否仍为 39/41，顺序和自检是否保持。
3. 小红书是否从 5/18 告警、301 行恢复到 0 告警和约 200 行。
4. 同一份设备缓存上，模糊锚点、stride3、多帧共识三项删除测试各自贡献多少；多数规则是否
   遇到“多次截断、一次完整”的反例。
5. 完整 4fps 模式产生的临时 JPEG 是否全部清理；实现会连续尝试 3 次，报告中的
   `remainingTempFileCount` 非 0 时显式告警，验收脚本拒绝通过。native 抽帧若中途失败也会
   重试删除半批文件；下一次流程开始前还会扫描专用缓存目录并拒绝带着删不掉的旧 JPEG 继续。

## 🔴 M3 第四轮：模糊锚点有效，但引入一处全盘回归（2026-08-28）

真机第二轮（Codex `b11c230`）在第三轮基础上加了模糊锚点 + stride 3 + 多帧共识，
**结果全面倒退**：

| 指标 | 第三轮真机 | 第四轮真机 | |
|---|---:|---:|---|
| 字符级 | 89.2% | **82.7%** | −6.5 |
| 发言人 | **39/41** | **25/41** | −14 条 |
| 输出条数 | 40 | 33 | −7（**全是「我」的消息**）|
| 微信耗时 | 4,501 ms | **32,965 ms** | 慢 7.3 倍 |
| 小红书耗时 | 5,987 ms | **42,607 ms** | **超 40 s 目标** |
| 局部采样 | 13 气泡 / 60 ms | **0 / 0 ms** | 完全未触发 |

### 根因：`findAlignPeaks` 缺一条几何约束

峰值取的是全局最高频的 `x` 与 `x+w`。stride 7→3 多取 20 帧，噪声行随之增多，
**最高频落到了屏幕中间一个反复出现的小元素上：右峰算成 123（屏幕宽 720）**。

后果是一条完整的链，真机现象逐项对得上：

```
右峰 123  →  rightHit() 几乎恒假
          →  双贴峰 0 个        → 局部采样一次都不触发（sampleRegionCount: 0）
          →  「我」的短消息既不贴左峰也不贴右峰，被整条丢弃（少 7 条）
          →  长消息只贴左峰，全判「对方」（发言人 25/41）
```

**离线验证脚本当时已修**（4d70d01）：左峰只在屏幕左半找、右峰只在右半找，单字碎片不参与投票。
第五轮复核确认 Android production helper 当时仍是旧实现；经 Alice 明确授权，本批把同一逻辑
同步到 `src/anchor-utils.js`。修后真机数据 stride=3 的右峰回到 584、双贴峰恢复 13 个、
字符级 82.7% → 89.2%、发言人 25/41 → 34/41（无采样；采样恢复后应回到 39/41）。
Mac Vision 回归不受影响：93.9% / 39/41 / 13 个气泡全部像素判定。

### 真机 OCR bundle：这一轮最有价值的产出

Codex 实现的 OCR 导出可用，格式与 `verify/` 缓存完全兼容，
转换后即可在 Mac 侧完整复现真机 ML Kit 行为：

```bash
python3 -c "
import json,os
d=json.load(open('<bundle>/M3-OCR-BUNDLE.json'))
os.makedirs('/tmp/mlkit_wx',exist_ok=True)
for f in d['frames']:
    json.dump(f['lines'], open('/tmp/mlkit_wx/'+f['file'].rsplit('.',1)[0]+'.json','w'), ensure_ascii=False)
"
node anchor-ocr.mjs /tmp/mlkit_wx wechat --stride 5
```

**以后所有参数调整都不必再占用 Alice 的真机时间**，只有最终验收需要真机。

### 用真机 ML Kit 数据横扫 stride（峰值修复前，故发言人偏低）

| stride | 微信字符级 | 微信发言人（无采样） | 小红书自检 | 小红书位移 |
|---:|---:|---:|---|---:|
| 3 | 89.2% | 34/41 | **42/42 通过** | **10,415**（Vision 基准 10,414）|
| 5 | **91.4%** | 35/41 | 2/25 告警 | 10,023 |
| 7 | 89.4% | 35/41 | 2/18 告警 | 9,713 |
| 9 | 89.6% | 34/41 | 3/14 告警 | 9,900 |

**两段素材的最优 stride 不同**：微信段 5（字符级 91.4%，**已过 90% 线**），
小红书段必须 3（只有它 0 告警且位移与 Vision 基准吻合）。
这不是「为不同 app 写专属启发式」——取帧密度本就该随内容滚动速度调整，
App 里已有 `PRESETS[app]` 承载这类参数。

### 还剩两件

1. **帧抽取性能回归**：第三轮 162 ms/帧（16 帧 2,598 ms），第四轮曾用 36 个算法帧作
   20,508 ms 的分母得到 570 ms/帧。**⚠️ 第五轮订正：单帧成本涨 3.5 倍不成立**；bundle
   证明 native 实际抽了完整 108 帧，真实值是 189.9 ms/帧。`OPTION_CLOSEST` 实现没有变化，
   超时来自完整 4fps 诊断采集默认开启，而不是“精确帧抽取”或 `maxAnchorGapMs` 本身。
2. **stride 按模式分**：微信 5、通用 3。

## 🟡 M3 第五轮候选：真机缓存先过闸，性能验收与完整诊断采集分离（2026-08-28）

**状态：`PARTIAL_EXEC`。** 两份 ML Kit 真机缓存、两份 Vision 缓存、稀疏反例和工程检查
已经通过；第五轮 APK 尚待 Alice 做唯一一次真机验收，因此这里不提前宣称总耗时和局部采样
已经通过。

### APK 前先用真机 ML Kit 缓存验收

| 素材 / 参数 | 自检 | 累积位移 | 内容量级 | 微信字符级 | 对齐峰 |
|---|---:|---:|---:|---:|---:|
| 微信 stride=5 | 21/21 | 3,744 | 40 行 / 815 字符 | **91.4%** | 左 136 / 右 584 |
| 小红书 stride=3 | **42/42** | **10,415** | 211 行 / 2,985 字符 | — | — |
| Vision 微信 stride=7 | 15/15 | 3,744 | 41 行 / 810 字符 | 93.9%、39/41 | 左 134 / 右 585 |
| Vision 小红书 stride=7 | 18/18 | 10,414 | 202 行 / 2,758 字符 | — | — |

微信离线缓存没有原始 JPEG，所以 35/41 只是纯坐标能力；设备验收仍必须由 13 个歧义气泡的
局部采样把已匹配消息补到 39/41。Vision 回归中 13 个气泡全部判定、未定 0。小红书真机
缓存比 Vision 多 9 行 / 227 字符；它没有 groundtruth，当前只能确认锚点、位移与内容量级，
不能把字符数差直接写成质量提升。

Android production helper 直接读取同一份 ML Kit 微信缓存，得到 22 帧、0 个锚点告警、
累计位移 3,744px、左峰 136 / 右峰 584、13 个局部采样区域。新增的 720px 宽合成回归中，
旧全局计数会落到左 250 / 右 123；同步后的几何约束得到左 134 / 右 585。

### 三项删除测试

1. **删除模糊锚点**：微信 stride=5 仍为 91.4%、21/21、3,744px，净字符贡献 0；小红书
   从 42/42、10,415px 退到 **38/42、10,176px**，新增 4 对告警并少算 239px。模糊匹配在
   ML Kit 抖动较大的通用内容上是必要条件。
2. **删除模式 stride**：微信把 5 改回 3，字符级从 91.4% 降到 **89.2%**（−2.2 点）；
   小红书把 3 改成 5，从 42/42、10,415px 退到 **23/25、10,023px**（2 对告警、少算
   392px）。因此微信 5、通用 3 分别进入 `PRESETS[app].anchorStride`，不是统一写死。
3. **删除“性能验收关闭完整诊断”**：第四轮开关开启时，微信实际抽/OCR 108 帧，总耗时
   32,965ms；小红书实际抽/OCR 128 帧，总耗时 42,607ms。第五轮关闭后只请求 22 / 43 帧。
   按同一轮真机的实际分项线性回算，预计约 **7,683ms / 15,461ms**；这是基于真实单位成本的
   估算，最终数字仍以第五轮真机报告为准。

### 20 秒抽帧不是单帧算法退化

第四轮 bundle 同时记录了物理抽取数、JS 阶段和 native 阶段：微信 20,508 / 20,506ms，
小红书 23,931 / 23,929ms，bridge 差额都只有 2ms。正确分母分别是 108 / 128：
189.9 / 187.0ms 每物理帧；相对第三轮 162.4ms 是约 15–17% 差异，不是 3.5 倍。
Kotlin 的 `MediaMetadataRetriever.OPTION_CLOSEST + JPEG 100` 循环从第三轮起没有改变，第四轮
只在其前后增加临时文件清理；清理仅 119 / 157ms，也解释不了 20 秒。

第五轮保留完整 4fps 导出开关，但默认关闭，并在报告里同时记录 `extractedFrameCount`、
`frameCount` 和 `nativeFrameExtractPerFrameMs`，防止再次拿算法子序列数充当抽帧分母。关闭
诊断不会退回同步关键帧：选中的 22 / 43 帧仍使用 `OPTION_CLOSEST` 精确时刻，并一直保留到
微信局部采样结束。

### 第五轮设备门禁

- 微信：4fps / stride=5 / 22 帧 / 最大间隔 1250ms；字符级 ≥90%、speaker 39/41、
  局部采样触发且未定 0、自检全过、总耗时 ≤40s。
- 通用：4fps / stride=3 / 43 帧 / 最大间隔 750ms；自检 0 告警、约 200 行、总耗时 ≤40s。
- 两段都要求完整 4fps 诊断关闭、实际抽取数等于算法帧数、临时 JPEG 残留为 0。
- 现有拼接路径、score 计算、groundtruth 和 iOS 均不改；Android production helper 的
  `findAlignPeaks` 只同步 4d70d01 已验证的几何约束和单字碎片排除，不另调参数。

## ✅ M3 验收通过（2026-08-28，OPPO / ML Kit，Alice 亲测，Codex `524f1f5`）

**五条 Done when 全部达成。**

| Done when | 目标 | 实测 | |
|---|---|---|---|
| 1. Node 侧复现 | 字符级 ≥93%、发言人 ≥39/41 | 93.9% / 39/41 | ✅ |
| 2. 小红书段通用模式 | 与现有架构同量级（199 行）| 202 行 | ✅ |
| 3. 失败自检 | 锚点不足必须告警 | 已多次实证有效 | ✅ |
| 4. 真机 | ≤40 s、字符级 ≥90%、发言人 ≥39/41 | **6.4 s / 91.4% / 39/41** | ✅ |
| 5. 与 M2-L3 逐字节比对 | 有差异则验质量未掉 | 各 15 行差异，**质量反升** | ✅ |

### 同机同 OCR 引擎的横向对比

| | 字符级 | 发言人 | 顺序 | 微信段总耗时 |
|---|---:|---:|---|---:|
| M2-L3 拼接架构 | 90.6% | 39/41 | 完全正确 | 161,600 ms |
| **M3 文本锚点** | **91.4%** | 39/41 | 完全正确 | **6,359 ms** |

**质量略升，耗时降到 1/25.4。** 小红书段 13,945 ms、自检 42/42 全过。

### 真机耗时分项（两段均对账通过）

| 阶段 | 微信（22 帧） | 小红书（43 帧） |
|---|---:|---:|
| 精确帧抽取 | 3,735 ms（**169.7 ms/帧**） | 7,578 ms（176.2 ms/帧） |
| 逐帧 ML Kit OCR | 2,299 ms | 5,625 ms |
| UI 让出 | 164 ms | 346 ms |
| 固定 UI / 锚点 / 去重 | 91 ms | 373 ms |
| **歧义气泡局部采样** | **55 ms（13 个全解决，未定 0）** | 0 ms（通用模式不判发言人） |
| Markdown + 清理 | 12 ms | 19 ms |
| **总计** | **6,359 ms** | **13,945 ms** |

第四轮的帧抽取性能回归已修复：570 ms/帧 → 169.7 ms/帧。
累积位移微信 3,744、小红书 10,415，与 Mac Vision 基准 3,744 / 10,414 吻合。

### 一条方法学收获：OCR bundle 让真机可在 Mac 侧复现

用第四轮导出的真机 bundle，在 Mac 侧预测 stride=5 的字符级为 **91.4%**，
真机实测**同样是 91.4%**。复现方法可靠，此后调参不必占用真机时间。

这条能力是第四轮那次全盘回归换来的：当时只在 Vision 数据上做回归
（噪声少、峰值永远不会算错），盲区正好盖住了 `findAlignPeaks` 的缺陷。
**现在「出 APK 前必须先用真机 bundle 在 Mac 侧验证」已是硬要求。**

### 已拍板（v0.3.0）：文本锚点转默认，拼接保留为回退

M3 五条 Done when 全部达成后，Alice 选择原方案 2：Android 默认使用文本锚点，原有拼接架构不删，继续作为处理路径开关中的回退。自检告警时，结果页直接说明“部分内容可能缺失”，并提供用拼接路径重新处理的入口。

**仍未验证的场景**：深色模式的气泡配色、其他 app 的通用模式表现。深色模式本批不调阈值、不做真机验收；颜色无法判断时跳过该气泡并明确告警，不能把“尚未验证”写成“支持”或“不可用”。另有一段 44.2 秒微信群聊临时实测，覆盖规模见下节；昵称已用该次导出的完整 bundle 做离线字段化验证，但没有字符级真值，也没有第二台设备或其他分辨率证据，不等于群聊已经完整适配。

## M3 上线批：v0.3.0

### 上线改动

1. Android 初始处理路径改为文本锚点；iOS 保持原有拼接路径，本批不动 iOS。用户仍可在“处理路径”中手动选择拼接。
2. 文本锚点结果为 `warning` 时，界面只显示普通用户需要知道的风险：“部分内容可能缺失”，并提供“一键用拼接路径重新处理”。锚点数、投票占比、RGB 等内部明细只留在完整验收报告。
3. 局部采样返回未定或错误时，不猜某一侧，也不输出该气泡；未定数大于 0 会进入 warning。三组不满足浅色阈值的构造 RGB 用例覆盖了“全部未定仍明确告警”路径。
4. 版本号统一升为 `0.3.0`。README 的产品说明、流程图和 M3 状态同步为“文本锚点默认 + 拼接回退”；深色模式只写“尚未真机验证”。
5. 真机发布验收只跑两段已验证的浅色素材：微信确认默认路径、13 个歧义气泡全部判定和耗时；小红书确认 42/42 自检。深色不占用本次真机轮次。

### 发布门禁

- 出 APK 前必须同时通过两份 OPPO/ML Kit bundle、两份 Mac Vision 回归、stride=12 稀疏反例、深色 RGB 行为测试、`npm test`、lint、文档一致性和 Expo export。
- APK 完成后记录构建状态、源提交、字节数和 SHA-256，并由 Alice 按 `verify/M3-DEVICE-ACCEPTANCE.md` 运行两段浅色素材。
- 只有两份真机报告通过，才创建 annotated `v0.3.0` tag 和 GitHub Release，并挂同一份已验 APK。

### v0.3.0 唯一候选 APK（待 Alice 两段浅色真机验收）

- EAS build ID：`d6dc7577-b0b1-4bef-ad55-8a7f1d6c3637`，状态 `FINISHED`，完成于 2026-08-28 15:28:02 UTC。
- 构建页：<https://expo.dev/accounts/aliceljy/projects/tenglu/builds/d6dc7577-b0b1-4bef-ad55-8a7f1d6c3637>。
- APK：<https://expo.dev/artifacts/eas/8pWQBkAyYMn3zh48YMIy7_1-RJziT19525P45-eK8ic.apk>。
- 源提交：`436c7b26509912d2074e81a53a7eecc83dbfbd44`；版本 `0.3.0`。
- 文件大小：`114584348` bytes（约 109.3 MiB）；SHA-256：
  `9ab21d768b1e1fb3ccb1f67e0f39d7568c5199fd5c67a199af66fb3b675b587d`。
- 本地下载与 ZIP/APK 完整性检查均退出码 0。它现在只是唯一候选；在两份真机报告通过前，不创建 tag 或 GitHub Release。

### 已知限制

深色模式尚未在真机验证。当前保护承诺是：判不出发言人时跳过该气泡、明确告警并建议用拼接路径重试；这不等于拼接路径已在深色模式验证，也不等于深色模式不可用。微信群聊已有一次 44.2 秒临时实测，昵称字段已用同次完整 bundle 离线验证，但仍没有字符级真值、第二台设备或其他分辨率证据；其他 app 仍没有真机覆盖。

## 群聊场景实测（2026-08-28，Alice 临时加测，不在 M3 Done when 内）

素材：微信群「🚀AI超元域会员专属交流群」44.2 s 录屏，177 源帧 @4fps，stride=5 取 36 帧。
真值来自 wechat MCP 直接导出数据库记录，录屏实际覆盖 **10:16–11:46 共 60 条**。

| 项 | 结果 |
|---|---|
| 总耗时 | 10,203 ms |
| 失败自检 | 35/35 对全过 |
| 累积位移 | 12,236 px |
| 歧义气泡 | 22 个 → **20 判定 / 2 未定** |
| 内容召回 | **59/60 有对应内容**，仅 1 条 OCR 彻底失败 |
| 输出 | 91 行 |

**没有字符级数字**：群聊屏幕上显示的是群昵称，而数据库导出的是 wxid，两者对不上，
无法构造逐字真值。只能算内容召回率。

### 发现 1：灰色气泡的保护机制真实触发，行为正确

2 个 RGB `(225,225,225)` 的灰底气泡，两条判据都不满足
（`G-B=0` 够不到 40，`225` 够不到 245 的白色门槛），采样返回「未定」。
**它没有猜，而是明确告警「气泡底色未判定，该气泡未输出」。**

这是为深色模式设计的那套降级**第一次在真实场景被触发**，验证有效。
代价是那 2 条内容被丢弃 —— 宁可不输出也不猜错，这个权衡是有意选的。

### 发现 2：昵称应拆成独立字段（Alice 指出）

当前输出把昵称与正文粘在一起：`超元域 艾森Eisenyoutube 上有爆料`。
**昵称在群聊里是必要内容，不是噪声** —— 单聊只有两方所以用 `[我]/[对方]` 标记就够，
群聊有 N 个人，OCR 读到昵称是正确行为。要改的是格式：
`[超元域 艾森Eisen] youtube上有爆料`。

⚠️ CC 起初把它判成「粘连缺陷」，是拿单聊的框架套群聊，被 Alice 当场纠正。

### 发现 3：失分几乎全在 OCR 识字，不在架构

9 条未完整召回的逐条核对后，8 条其实都召回了、只是 ML Kit 认错一两个字
（硬→便、懂→憧、接→援、成→咸、甩→用、赚→賺）；
只有「万达高管低了。。。」一条 OCR 彻底崩成「刀込同官」ooo」。
位移、去重、自检全部正常。

### 方法论教训：同一个问题算出三个数，说明方法有问题

CC 在本次比对里连错两次：逐行匹配得 **91.7%**（偏宽松），
改全文子串匹配得 **76.7%** —— 根因是 `difflib.SequenceMatcher` 默认
`autojunk=True`，会把出现频率 >1% 的字符当垃圾忽略，blob 一长，
「哈」「的」这类高频汉字全被丢掉，「哈哈哈」因此匹配出 0.0%。
关掉 autojunk 后得 **85.0%**，再逐条人工核对才确认实际是 59/60。

**两个相差 15 个点的数字，任何一个都不该报出去。** 差异本身就是方法有病的信号。

### v0.3.0 处置：群昵称独立为发言人字段

同次导出的 177 帧 ML Kit bundle 在 Mac 侧按真机参数重建：stride=5 取 36 帧，
自检 35/35、累积位移 12,236 px、去重后 239 行，均与真机报告一致。昵称只用几何识别：
独立顶行位于气泡左缘，正文首行向右缩进且字框明显更高；不查昵称词表，也不按冒号或空格猜。

- 高置信昵称—正文对 61 个；长正文框向左扩张的严格第二档再增加 4 个，共 **65 个**。
- 其中 4 个昵称位于旧聚行组内部；输出层把它们单列，但发言人仍沿用原气泡判定，局部采样仍是 22 次，未定仍是原来的 2 个。
- 本次收到的诊断 bundle 自带报告是 93 行（上一节 91 行来自较早的普通真机运行，两次 OCR 输出有轻微抖动）；字段化后是 97 行。把 `[昵称] 正文` 可逆还原为 `昵称正文` 后，**1,976 个 OCR 字符逐字节完全相同**，证明字段化没有删掉这份 bundle 已有的 OCR 内容。上一节 59/60 仍是较早普通真机运行的人工核对结论；这份诊断 bundle 没有重新做消息级真值评分，不能把两个运行的数字合并。
- 删除测试：去掉长正文第二档是 61 个字段；去掉组内扫描是 61 个字段且少 4 条独立输出；全部关闭则是 0 个字段并恢复旧的 93 行粘连格式。
- 同一批微信单聊的 ML Kit 与 Vision 数据均命中 0 个昵称候选，字符级仍为 91.4% / 93.9%，Vision 发言人仍为 39/41 满分。

这仍是离线复现，不是新 APK 的群聊真机复验。数据只来自 720×1652 的一台设备，且没有可评分的昵称字符真值；因此这里只能证明字段分离不丢已有 OCR 内容，不能宣称跨设备群聊已全面适配。
