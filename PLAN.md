# 誊录（Tenglu）— 施工图纸 v1

> 名字取自宋代科举的誊录制度：专人把考生原卷**一字不改地抄一遍**。
> 这正是本产品的全部承诺——只忠实还原，不加分析。名字待 Alice 确认，不影响施工。

## 一句话

把一段手机录屏，变成一份干净的 Markdown 记录。全程在手机本地完成，不联网、不调 API、不需要任何 key。

## 产品边界（已拍板，不要越界）

- **只做到"交出记录"**。不做分析、不做总结、不做洞察。用户拿这份 Markdown 去喂任何 AI，那之后的事与本产品无关。
- **不做结构化解析**。OCR 出来的文字按位置排好序就输出，不去判断"这是昵称/这是点赞数/这是回复谁"。
  理由：读它的 AI 比正则更擅长这件事，而且我们一判断就会判错——2026-08-28 实测，精细解析 48 条评论错 4 条，全文本模式零错。
- **微信是唯一做左右区分的场景**（气泡颜色分我方/对方）。其它 app 一律纯文本。

## 已验证的事实（2026-08-28 实测，不要重新验证，也不要推翻）

用 Alice 真实录屏 + 微信解密记录做基准：

| 项 | 数字 |
|---|---|
| 输入 | 微信单聊录屏 27s / 720×1652 / 120fps / 2757 帧 |
| 实际需要的帧数 | **6 帧** |
| 位移估计（纯 JS，两级搜索） | **8–15 ms / 帧对**，5 对共 38–75 ms（Mac / Node 26） |
| 拼接长图 | 720×5108 |
| OCR（macOS Vision，分段 1400px + 重叠 200px） | 约 15 s |
| **逐字完全一致** | **37 / 41 条 = 90.2%** |
| 发言人判对 | 39 / 41 = 95.1% |
| 消息顺序 | 100% 正确 |

小红书笔记+评论录屏（32s / 2184 帧）同一套算法：位移估计置信度中位 0.996、最低 0.934，拼出 720×11738 长图，183 行 2489 字。
**结论：拼接层跨 app 通用，唯一要改的是固定 UI 的取样范围。**

## 核心算法（已验证，照抄，不要重新设计）

### 1. 分离固定 UI 与滚动区

对若干帧做逐行跨帧标准差 `row_std[y]`：

- `row_std < 3` → 固定 UI（导航栏、输入栏）
- `3 ≤ row_std < 12` → 微动（状态栏时钟、电量、录屏计时胶囊）
- `row_std ≥ 12` → 滚动内容区

取中间最大的连续滚动段作为有效区。微信实测 y153–1521，小红书 y183–1508。

⚠️ **取样必须用"已经稳定滚起来"的那段帧，不能用整段视频。**
小红书顶部导航栏是滚动时才浮现的，用整段测会把它误判成滚动区（实测：用全部 128 帧测得"0–1508 全是滚动"，只用后 68 帧测才正确分出 68–181 固定）。

### 2. 位移估计（两级搜索）

```js
const DS = 4;        // 粗搜降采样倍数
const OFF = 60;      // 模板在「新帧」中的起始 y
const BAND = 300;    // 模板高度
const R = 24;        // 精搜半径

// 粗搜：降采样 4 倍，全范围 SAD 找最小
// 精搜：全分辨率，在粗搜结果 ±R 内再找一次
// 返回：滚动量 = 匹配到的 y − OFF
```

判据用 **SAD（绝对差和）**，不用归一化互相关——同一段视频的帧是同源的，不存在亮度差，SAD 更快且实测精度相同。

⚠️ **直接匹配相隔较远的两个关键帧，不要逐帧累积。**
逐帧累积每帧半像素误差，22 帧就漂 3px；一次性匹配没有累积误差。实测两者拼出的图跑同一套 OCR，一次性匹配的假消息更少。

### 3. 帧选择

目标：相邻关键帧之间前进约 **60% 可视区高**，保证足够重叠。

实测 6 帧的位移是 [832, 833, 822, 864, 388]，可视区高 1369，即每次前进约 61%。

⚠️ **回弹帧必须丢弃，不能当作 0 位移继续贴。**
滑到底部的橡皮筋回弹会产生负位移；若按 `max(0, dy)` 处理成 0 却仍然贴上去，会用错位画面覆盖正确内容，同一条消息出现两次。实测这个 bug 让准确率从 90.2% 掉到 87.8%。正确做法：**负位移的帧直接跳过。**

### 4. 拼接

画布高 = 累积位移 + 可视区高。按累积位移逐帧覆盖贴上，后帧覆盖前帧。

### 5. OCR

分段 **1400px + 重叠 200px**，不要整张长图一次识别——实测整图 62 块 vs 分段 73 块，整图有精度损失。

分段结果**按位置去重，不要按文本相等去重**：跨段 OCR 结果常不一致（同一行一段识别出"是的"、另一段只出"是"），按文本比对会漏掉，然后被后续合并逻辑拼成"是的是"。
规则：`|Δy| < 20 且 |Δx| < 40` 视为同一行，保留文本更长的那次。

### 6. 输出

- **微信模式**：采文字块所在气泡的底色（取块内亮度 >150 像素的中位 RGB）；`G − B > 40` → 我方（微信绿），纯白 → 对方。同气泡续行合并条件：同一侧 + `0 ≤ Δy < 62` + `|Δx| < 60`。
- **通用模式**：按 y 排序，`|Δy| < 18` 视为同一行、按 x 拼接，直接输出。不做任何字段判断。

## 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 客户端 | Expo RN SDK 57 | 复用脉络项目已跑通的栈 |
| 选视频 | `expo-image-picker` | 标准 API，用户点一下授权 |
| 抽帧 | `expo-video-thumbnails` | 按时间点抽帧；⚠️ 一次一帧，需多次调用 |
| 位移+拼接 | **纯 JS，无原生模块** | 已实测 8–15ms/对，手机慢 5 倍也才 ~75ms |
| OCR | `@react-native-ml-kit/text-recognition` | 安卓 ML Kit，离线免费中文；⚠️ 需 dev build，不能用 Expo Go |
| 出包 | EAS 云构建 APK | 本机无 Xcode，安卓走 EAS（token 在 mini `~/.expo-token`） |

**不需要**：后端、服务器、API key、订阅、账号、云存储、任何联网能力。

## 里程碑

- **M1（当前批）**：能跑通的最小 APK。选视频 → 16 帧预扫定滚动区 → `walkKeyframes` 边抽边验取关键帧 → 拼接 → OCR → Markdown → 复制到剪贴板，显示每步耗时。**含通用 / 微信两种输出模式**（验收要验发言人 95.1%，没有微信模式就验不了）。不做界面美化、不做 iOS、不做多 app 专用规则。
- **M2**：保存为文件 + 分享。抽帧成本优化（预扫的 16 帧能否复用为关键帧候选）。
- **M3**：iOS（Vision OCR）+ 界面。

> ⚠️ 本节 2026-08-28 05:30 修订过。原文写的是"M1 固定抽 12 帧、自适应留到 M2、双模式留到 M2"，
> 那是在做试探法验证**之前**写的；后来实测推翻了均匀抽帧（见下方"已证伪"），`stitcher.js`
> 也已改为 `walkKeyframes`，但本节漏改，被 Codex 在动工前拦下。三份材料现已对齐。

## 已证伪（别再走回头路）

**均匀稀疏抽帧不可用。** 曾经的方案是"按固定间隔抽 N 帧，算位移后丢弃冗余帧"，实测三种失败：

| 抽帧数 | 结果 |
|---|---|
| 8 | 滚动区检测**完全失败**（y154~1522 被误判成 y761~1385，帧太少时跨帧方差不可靠） |
| 12 | 长图 5592 vs 正确 5107，**多出 485px 重影**——稀疏帧跨过回弹段，匹配到错误位置，总位移被高估 |
| 16 | 正确，746ms |
| 108（全量） | 正确，4692ms |

现行方案是 **16 帧预扫定滚动区 + `walkKeyframes` 边抽边验**：探测 11 次拿到 7 个关键帧，568ms，
结果与全量处理差 1px。每次匹配都是关键帧对关键帧，无累积误差。

**位移上限 0.65 是实测定的**，同一实现在不同位移下的准确性：

| 位移占视口 | 误差 | conf |
|---|---|---|
| ≤70% | **0px** | 1.000 |
| 75% | -18px | 0.772 |
| ≥80% | **-714px（完全翻车）** | 0.69-0.78 |

好消息是 conf 在翻车时会掉到 0.85 以下，`walkKeyframes` 的 `conf < 0.85` 判据能兜住；
但把目标区间压在安全区（0.45–0.65）比依赖兜底更稳。
（Minis 在 iSH 侧用 `apple-vision overlap` 测到同类边界，它那边更早——62.5% 就翻车，
且 **confidence 恒为 1.0 完全不报警**，所以那条链路必须靠位移合理性做兜底，不能信 conf。）

## 中间盘图片格式：用 BMP，不要 JPEG

同一张长图送同一个 OCR：

| 格式 | OCR 块数 | 总字符 | 编码耗时 |
|---|---|---|---|
| PNG（无损） | 71 | 661 | 慢（zlib） |
| **BMP（无损）** | **71** | **661** | **11ms** |
| JPEG q92 | 69 | 655 | 44ms |
| JPEG q88 | 69 | 654 | 44ms |

**诚实说明**：JPEG 少认的那 2 块在这段素材上恰好不影响最终准确率（93.9% vs 93.7%，
在 93.7–94.0% 的正常抖动区间内）。**选无损是出于原理——不引入不可预测的失败模式——
而不是出于实测收益。** BMP 编码比 JPEG 还快，代价只是中间文件大（10MB vs 0.5MB），临时文件无所谓。

`src/stitcher.js` 的 `encodeBMP()` 已实现（54 字节头 + 倒序写像素，24bpp，4 字节行对齐）。
Minis 在 iSH 侧独立测到同样现象，并额外测出 PNG 编码在模拟层比 BMP 慢 53 倍。

## 已知未解风险（M1 要回答的）

1. **抽帧速度未知**。`expo-video-thumbnails` 一次一帧，抽 N 帧要调 N 次。
   现行流程需要 16（预扫）+ 约 11（试探）= 约 27 次抽帧。若单次抽帧超过 300ms，
   总耗时会不可接受，那时优先考虑把预扫的 16 帧复用为试探候选（M2 项提前）。
2. **ML Kit 在长图上的表现未验证**。长图可能有几千像素高，需按第 5 节分段。
3. **内存**：720×11738 的 RGBA 约 34MB，安卓中端机应可承受，但要实测。
